meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: "export DATABASE_URL=file:./dev.db && npm run build:full"

run:
  command: "npm start"
  containerPort: 8080
