meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  additionalCommands: "npm ci && export DATABASE_URL=file:./build-temp.db && npm run build:full"

run:
  persistenceMount: /data
  command: "npm start"
  containerPort: 8080
