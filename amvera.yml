meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: "npm ci && export DATABASE_URL=file:./build-temp.db && export VITE_SOLO_COOP_PUBLIC=false && npm run build:full"

run:
  persistenceMount: /data
  command: "npm start"
  containerPort: 8080
